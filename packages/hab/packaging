#!/bin/bash
set -ex

HAB_VERSION=0.30.2-20170822232349

tar xfv hab/hab-${HAB_VERSION}-x86_64-linux.tar.gz
cd hab-${HAB_VERSION}-x86_64-linux

mkdir -p ${BOSH_INSTALL_TARGET}/bin
cp hab ${BOSH_INSTALL_TARGET}/bin
